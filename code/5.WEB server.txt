# Apache 웹 서버 설치
dnf install httpd -y

# 커스텀 웹 페이지 생성
echo "<h1>🐧 Welcome to 4RSENAL Rocky Linux Web Server!</h1>" > /var/share/nginx/html/index.html # 또는 /var/www/html/index.html
echo "<h1>🐧 Welcome to 4RSENAL Rocky Linux Web Server!</h1>" > /var/www/html/index.html

# 서비스 활성화 및 80 포트 외부 방화벽 해제
systemctl enable --now httpd
firewall-cmd --permanent --add-service=http
firewall-cmd --reload